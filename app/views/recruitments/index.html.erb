<%= render "shared/header" %>
<h1>現在掲載中の求人情報</h1>
<div><% @recruitments.each do |recruitment| %>
  <div><%=link_to "#{recruitment.work_day} #{recruitment.category}の#{recruitment.work}を募集", company_recruitment_path(@company,recruitment.id)%>
    <%= link_to "募集の取り下げ", "#"%>
    <% end %>
  </div>
</div>
<%= link_to "新しく求人を募集する", new_company_recruitment_path(@company) %>